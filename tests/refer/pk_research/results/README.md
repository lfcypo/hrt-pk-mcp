# Results

Place generated fitting outputs here.

Suggested contents per run:

- normalized anchor export
- fitted parameter JSON
- error summary CSV
- plot images comparing fitted curves vs anchor points

Do not treat anything in this folder as approved runtime truth until the corresponding constants are reviewed and copied
into `PKSharedCatalog.json`.
